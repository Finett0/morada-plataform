import { PagePlaceholder } from '@/components/global/PagePlaceholder';

export default function LoginPage() {
  return <PagePlaceholder eyebrow="Acesso" title="Entrar" spec="SPEC §2.1 · issue 03" />;
}
