import { PagePlaceholder } from '@/components/global/PagePlaceholder';

export default function SignupPage() {
  return (
    <PagePlaceholder eyebrow="Acesso" title="Criar conta da empresa" spec="SPEC §2.2 · issue 04" />
  );
}
