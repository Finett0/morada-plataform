import { PagePlaceholder } from '@/components/global/PagePlaceholder';

export default function DashboardPage() {
  return <PagePlaceholder eyebrow="Visão geral" title="Dashboard" spec="SPEC §2.6 · issue 08" />;
}
