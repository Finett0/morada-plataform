import { PagePlaceholder } from '@/components/global/PagePlaceholder';

export default function ContratosPage() {
  return <PagePlaceholder eyebrow="Garantias" title="Contratos" spec="SPEC §2.11 · issue 13" />;
}
